{
  "name": "CNFRCWebserver",
  "version": "0.0.1",
  "description": "Server for FRC team 4618",
  "main": "server.js",
  "private": true,
  "scripts": {
    "compilestart": "(babel ./src --out-file server.js --modules ignore) && (uglifyjs server.js --compress --mangle -o server.js) && (node server.js)",
    "start": "node server.js"
  },
  "author": "Jonathan Casella (Helios41)",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.1",
    "socket.io": "^1.3.6"
  },
  "repository": {
    "name": "4618-server",
    "version": "0.0.1",
    "private": true
  }
}
